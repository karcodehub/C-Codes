#include<stdio.h>

//Mathematically correlation between two signals s[n] and h[n]
void main()
{
    //cor arry store corelation betw orginal signal arry and shift_signal arry when shift is applied 
    int signal[][], shift_signal[][], cor[], shift[];

    //define signal and shit_signal
    int i=0,j=0,n=0,s=0, x=0,y=0;
    getch();
    printf("Enter value for signal in x rows and y colms\n");
    scanf("%d%d",&x,&y);
    for(i=0;i<x;i++)
    {   for(j=0;j<y;j++)
            {
                scanf("%d",&signal[i][j]);
             }
    }

    printf("Enter value for shift_signal\n");
    for(i=0;i<x;i++)
    {   for(j=0;j<y;j++)
            {
                scanf("%d",&shift_signal[i][j]);
		temp_shift[i][j]=shift_signal[i][j];

             }
    }

     printf("Enter value for number of shift\n");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&shift[i]);
    }                                                                                                                                                                      for(i=0;i<n;i++)
     {
         printf("the signal is [%d]\n",signal);
         printf("the shift_signal is [%d]\n",shift_signal);
         printf("the shift is [%d]\n",shift);
     }

    //Selecting the time shift for shift_signal

}


for (i=0;i<n;i++)
{   
    s=shift[i];//Selecting the time shift for shift_signal
    for(j=0;j<x;j++) //to access for shift_signal arry
    {
       temp_shift[j][0]=(shift_signal[j][0]+s); // h[m − n] accessing only x axis value where as y axis is constant
       
     }
	for(k=0;k<x;k++)
	{
	  temp=temp_shift[k][0];
	  for(j=0;j<x;j++)
		{ 	
		
	         if (temp_shift[k][0]==signal[j][0])
                  {
                	cor[j]+=shift_signal[j][1]*signal[j][1]; //s[m] · h[m − n] accessing only y axis value to multiply
            	  }
		}
}
     print("The corelation signal is %d",cor[j]);
}
  
    
      
